# NOTE: runpod==1.6.2 is installed --no-deps in Dockerfile (fastapi[all] conflicts with supabase httpx)
# Minimal runpod runtime deps for serverless handler:
aiohttp>=3.9.0
backoff>=2.2.1
colorama>=0.2.5

# Supabase Python client (uploads output files, updates room status)
supabase==2.3.0

# HTTP client
requests

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# OpenCV for panorama stitching and image operations
opencv-python-headless>=4.9.0

# fal.ai client for ControlNet depth estimation
fal-client>=0.10.0

# PLY file parsing for gaussian splat conversion
plyfile

# 360-degree image utilities
py360convert

# Progress bars (for debugging/logging long operations)
tqdm
